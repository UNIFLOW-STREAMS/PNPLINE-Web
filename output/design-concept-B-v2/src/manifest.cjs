const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');const root=path.resolve(__dirname,'..');function walk(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap(d=>d.isDirectory()?walk(path.join(dir,d.name)):[path.join(dir,d.name)]);}const files=walk(root).filter(p=>!['manifest.json','preview-server.json'].includes(path.basename(p))).map(p=>{const b=fs.readFileSync(p);const o={file:path.relative(root,p).replaceAll('\\','/'),bytes:b.length,sha256:crypto.createHash('sha256').update(b).digest('hex')};if(path.extname(p)==='.png'){o.width=b.readUInt32BE(16);o.height=b.readUInt32BE(20);}return o;});const m={created:new Date().toISOString(),files};fs.writeFileSync(path.join(root,'manifest.json'),JSON.stringify(m,null,2));console.log(JSON.stringify({files:files.length,pngs:files.filter(f=>f.file.endsWith('.png')).length},null,2));
