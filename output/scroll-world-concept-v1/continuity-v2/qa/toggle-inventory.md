# Animatic overlay control QA

- Desktop (1440×1000): `문구 표시` must reveal a clearly visible caption inside the active frame.
- Desktop (1440×1000): `로고 보호 영역` must reveal the protected rectangles and an explicit active-state badge.
- Mobile (390×844): both controls must remain visible, tappable, and produce the same feedback.
- Switching orientation must preserve working controls for the newly rendered active frame.
- The static journey controls and animatic controls must operate independently.

